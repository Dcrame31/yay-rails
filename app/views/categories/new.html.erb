<h1>New Category</h1>
<%= form_for @user do |f| %>
<%= f.label :name %>
<%= f.fields_for :categories, @user.categories.build do |categories_fields| %>
    <%= categories_fields.text_field :name %><br>
    <% end %>
<%= f.submit 'Create Category' %><br>
<% end %>

<p><%= link_to 'Home', root_path %></p>