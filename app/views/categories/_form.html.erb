<%= form_for @category do |f| %>
<%= f.text_field :name %>
<%= f.hidden_field :user_id, :value => current_user.id %><br>
<%= f.submit category.persisted? ? 'Update Category': 'Create Category' %><br>
<% end %>