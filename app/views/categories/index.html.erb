<h1>Categories</h1>

<table class="table">
    <thead>
    <tbody>
<% @category.each do |category| %>
        <tr>
            <td>
                <%= link_to category.name, category %>
            </td>
        </tr>
<% end %>
    </tbody>
</table>
